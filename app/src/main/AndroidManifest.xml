<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.stechapps.animationtrials">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OnDragShow"></activity>
        <activity android:name=".ParralxUsingLibrary" />
        <activity android:name=".RippleView" />
        <activity android:name=".TypeWriter" />
        <activity android:name=".StagerLayoutTest" />
        <activity android:name=".NightLight" />
        <activity android:name=".Drag" />
        <activity android:name=".HingeEffect" />
        <activity android:name=".ColorAnimation" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>